# 템바보드 재단 계산기

고객이 입력한 치수를 원장 재단 지시서로 변환하는 단일 페이지 웹 계산기.
결과는 네이버 배송메모 / 오늘의집 주문서에 붙여넣을 텍스트로 출력한다.

---

## 1. 제품 사양

### 모양별 원장 규격

| 모양 | 스토어 옵션명 | 유효폭 | 원장 길이 |
|---|---|---|---|
| 반달 | 템바보드 타입 / 반달 | 295 mm | 2440 mm |
| 사각 | 템바보드 타입 / 사각 | 300 mm | 2440 mm |

내부 키는 `half` / `square`. 예전 표기 `원형`·`round` 로 들어온 입력도 반달로 처리한다.

유효폭 = 실측폭. 옆판 겹침 손실 없음.

### 색상 목록

**취급 색상이 모양마다 다르다. 사각 14색 / 반달 6색.**
스토어 색상 옵션명은 `코드-이름` 형식(예: `JA3011-화이트`)이다.

색상 데이터는 아래 구조로 별도 파일(`colors.js`)로 분리한다.
해당 모양의 코드가 있으면 그 모양에서 주문 가능한 색이다:

```js
{ name: "카키", codes: { square: "JA2210", half: "JA2210" } }
```

| 색상 이름 | 사각 코드 | 반달 코드 |
|---|---|---|
| 화이트 | JA3011 | JA3011 |
| 중간회색 | JA3024 | — |
| 연한회색 | JA3048 | — |
| 연한블루 | JA3054 | — |
| 진한블루 | JA3050 | JA3054 ⚠️ |
| 진한회색 | JA2205 | — |
| 카키 | JA2210 | JA2210 |
| 핑크 | JA3034 | — |
| 연한오크 | JA8401 | JA8401 |
| 중간오크 | JA5021 | — |
| 진한오크 | JA705 | JA705 |
| 밝은오크 | JA6W2 | — |
| 연한티크 | JA5033 | — |
| 진한티크 | JA0901 | JA0901 |

- 정렬 순서는 위 표(= 사각 옵션 목록) 순서를 따른다.
- ⚠️ `JA3054` 가 사각에서는 **연한블루**, 반달에서는 **진한블루**로 엇갈린다. 확인 필요.
- 반달에 없는 색을 반달로 주문하면 `COLOR_NOT_IN_SHAPE` 오류.

### 마감몰딩

| 규격 |
|---|
| 15 × 2440 × 12t |
| 15 × 1200 × 12t |

- 몰딩 색상은 보드 색상을 자동으로 따라간다. 별도 선택 UI 없음.
- 종류: 민자

---

## 2. 계산 로직

### 2.1 몰딩 적용 범위

**몰딩은 상단 한 면에만 붙는다.** 좌·우·하단은 고려하지 않는다.

따라서:
- 세로 차감은 **15mm 1회만**
- 가로 폭은 몰딩의 영향을 받지 않음

### 2.2 완성 치수 / 재단 길이 (세로)

재단하면 조각 세로가 **5mm 줄어든다.** 조각 1개당 1회 발생.
완성 치수를 맞추려면 그만큼 크게 잘라야 한다.

```
완성 치수 F = 고객 입력 세로
몰딩 사용 시  → F = F - 15
몰딩 미사용 시 → F = 그대로

재단 길이 L = F + 5      ← 원장에서 실제로 잘라내는 길이 = 원장 점유 길이
```

**재단 지시서에 찍히는 값은 재단 길이 L(= 톱을 넣는 길이)이다.** 완성 치수 F가 아니다.
2.5 이후의 계산은 전부 L 기준.

### 2.2b 세로 나누기 (유효길이 초과)

**세로에 상한을 두지 않는다.** 유효길이를 넘으면 나눠서 이어 붙인다.

```
조각 하나의 최대 완성 길이 = 유효길이 - 5
앞 조각부터 최대로 채우고, 마지막 조각이 나머지를 갖는다 (이음 최소)
```

예) 세로 3000, 몰딩 O, 끝단정리 X
→ 완성 2985 = **2435 + 550** (재단 2440 + 555), 이음 1군데

조각마다 재단 길이가 다르므로 원장당 개수·소요 원장도 조각별로 따로 센다.

### 2.3 가로 채우기 (원장 + 100mm 낱개)

원장으로 채울 수 있는 만큼 채우고, **남는 폭은 100mm 낱개 템바로 메운다.**

```
원장 장수 = floor(가로 / 유효폭)
남는 폭   = 가로 - 원장 장수 × 유효폭
낱개 개수 = ceil(남는 폭 / 100)

단, 낱개 개수 × 100 ≥ 유효폭 이면 → 원장 1장으로 대신한다
```

자재량은 같거나 적으면서 개수는 적기 때문. 낱개는 최대 `유효폭 - 100` 폭까지만 쓴다
(사각 200 / 반달 195). 예) 사각 남는 폭 250 → 낱개 3개(300) 대신 **원장 1장**.

마지막 낱개는 폭을 잘라 쓰고 자투리는 버린다.

| 입력 | 결과 |
|---|---|
| 사각 400 | 300 × 1장 + 100 × 1장 |
| 사각 1200 | 300 × 4장 (딱 떨어져 낱개 없음) |
| 사각 1450 | 300 × 5장 (남는 폭 250 → 원장 1장, 폭 자투리 50) |
| 반달 1200 | 295 × 4장 + 100 × 1장 (폭 자투리 80) |

낱개 템바 규격: 반달 `100x2440x9T` / 사각 `100x2440x12T`. 색상은 원장 색상을 따라간다.

가로가 유효폭보다 좁아도 같은 규칙이다. 예) 사각 250 → 원장 1장, 사각 150 → 낱개 2개.

### 2.4 원장 유효 길이

원장 끝단 정리 컷은 **고객(사용자)이 선택**한다.

```
끝단 정리 O → 유효길이 = 2440 - 20 = 2420
끝단 정리 X → 유효길이 = 2440
```

### 2.5 원장 1개당 뽑을 수 있는 개수

톱날 손실(커프)은 **재단 1회당 20mm**, 조각과 조각 사이에만 발생한다.

```
n × L + (n-1) × 20 ≤ 유효길이
n = floor((유효길이 + 20) / (L + 20))
```

검증 (끝단 정리 X, 유효길이 2440):
- L = 1985 → floor(2460 / 2005) = **1개**
- L = 800 → floor(2460 / 820) = **3개**  (800+20+800+20+800 = 2440)

검증 (끝단 정리 O, 유효길이 2420):
- L = 800 → floor(2440 / 820) = **2개**

⚠️ 위 검증값의 L은 **재단 길이**다. 고객 입력 세로가 아니다.
예) 몰딩 X, 세로 800 주문 → 완성 800 / 재단 805 → floor(2460 / 825) = **2개**

### 2.6 소요 원장 수

```
소요 원장 = ceil(필요 장수 / 원장당 개수)
```

### 2.7 재단 횟수 (스토어 '재단 필요해요' 수량)

```
재단 횟수 = floor(원장 유효길이 / 재단 길이) × 가로 장수
```

세로가 나눠진 경우 조각마다 계산해서 더한다.

예) 사각 1200 × 800
- 2440에서 800짜리 → 3회
- 가로 1200 → 300 × 4장
- 3 × 4 = **12회**

### 2.8 몰딩 수량

몰딩은 상단에만 붙으므로 **필요 길이 = 고객 입력 가로**.

이 길이를 2440짜리와 1200짜리 조합으로 덮는다.

```
기준 1순위: 총 개수 최소
기준 2순위: 개수가 같으면 자투리(남는 길이) 최소
```

예시:
| 필요 폭 | 결과 |
|---|---|
| 1200 | 1200 × 1개 |
| 2000 | 2440 × 1개 |
| 2500 | 2440 × 1개 + 1200 × 1개 (2개) |
| 3700 | 2440 × 1개 + 1200 × 2개? / 2440 × 2개? → ⚠️ 확인 필요 |

몰딩 이음부의 커프 손실은 계산하지 않는다.

### 2.9 소/대 구분

**없음.** 모든 조각은 동일 등급으로 취급한다.

---

## 3. 출력 형식

### 주문 요약

**스토어 옵션명 그대로 적는다.** 주문서와 한 줄씩 대조하기 위함.
원장은 옵션조합(타입 + 색상) 한 줄, 마감몰딩은 추가상품 규격별 한 줄.

```
템바보드 타입 / 반달, 색상 / JA3011-화이트 : 5개
템바보드 타입 / 사각, 색상 / JA6W2-밝은오크 : 4개
마감몰딩 2440mm 기준 : 1개
마감몰딩 1200mm 기준 : 2개 (카키 1, 밝은오크 1)
```

몰딩 색상은 보드 색상을 따라가는데 스토어 옵션에는 색 구분이 없다.
그래서 같은 규격에 색이 둘 이상 섞이면 괄호로 내역을 덧붙인다.

옵션명은 `store-options.js` 에 모아 둔다. 옵션명이 바뀌면 그 파일만 고친다.

### 재단 지시서

작업장용. 여기서는 색상 이름만 쓴다(코드 없음).

```
반달-화이트 : 1990 X 5컷
사각-화이트 : 1205 X 3컷
```

### 자투리

```
--- 자투리 ---
반달-화이트 : 가로 295 X 세로 450, 5개 (세로 자르고 남은 부분)
반달-화이트 : 가로 275 X 세로 1985, 1개 (가로 자르고 남은 부분)
마감몰딩-화이트 : 남는 길이 없음
```

### 공통 규칙

- **동일 사양(모양 + 색상 + 재단길이)은 합산**하여 한 줄로 출력
- **정렬: 색상별**
- 재단 지시서에 클립보드 복사 버튼 필요

---

## 4. 테스트 케이스

반드시 아래 케이스를 테스트 코드로 작성한다.

### TC-1 (기본)
**입력:** 반달 / 화이트 / 1200 × 2000 / 몰딩 O / 끝단정리 X

(원래 연한회색이었으나 연한회색은 **사각 전용**이라 반달로 주문할 수 없다. 계산값은 동일.)

| 단계 | 계산 | 결과 |
|---|---|---|
| 완성 치수 | 2000 − 15 | 1985 |
| 재단 길이 | 1985 + 5 | 1990 |
| 가로 채우기 | floor(1200/295) = 4, 남는 폭 20 | 원장 4 + 낱개 1 |
| 원장당 개수 | floor(2460 / 2010) | 1 |
| 소요 수량 | 원장 ceil(4/1), 낱개 ceil(1/1) | 원장 4 + 낱개 1 |
| 재단 횟수 | floor(2440/1990) × 5 | 5 |
| 몰딩 | 폭 1200 | 1200 × 1개 |

**출력:**
```
반달-화이트 : 1990 X 4컷
반달-화이트 100폭 : 1990 X 1컷
```

### TC-2 (원장당 다개수)
**입력:** 반달 / 화이트 / 590 × 800 / 몰딩 X / 끝단정리 X

- 완성 800 / 재단 805, 가로 590 = 295 × 2 (낱개 없음)
- 원장당 floor(2460/825) = 2
- 소요 원장 ceil(2/2) = **1장**
- 재단 횟수 floor(2440/805) × 2 = 6

### TC-3 (끝단 정리 영향)
TC-2와 동일하되 끝단정리 O → 원장당 floor(2440/825) = 2 → 소요 원장 1장

끝단정리가 원장당 개수를 가르는 구간: 세로 795(몰딩 X, 재단 800)
→ 끝단정리 X 는 3개, O 는 2개

### TC-4 (사각 유효폭)
**입력:** 사각 / 화이트 / 1200 × 2000 / 몰딩 O
- 사각 1200 = 300 × 4장, 낱개 없음
- 반달 1200 = 295 × 4장 + 낱개 1개 — 유효폭 차이 확인

### TC-5 (합산)
동일 모양·색상·재단길이 항목 2건 입력 시 한 줄로 합산 출력되는지 확인

---

## 5. 🚧 미확정

### (1) 색상 코드 — 해결됨 (2026-08-19)
스토어 제품 옵션 목록으로 사각 14색 / 반달 6색 확정. 주문 요약에 코드를 표기한다.
남은 문제 하나: **`JA3054` 가 사각 연한블루 / 반달 진한블루로 엇갈린다.**
반달 목록에 `JA3050` 이 없으므로 둘 중 하나는 오기일 가능성. 확인 필요.

### (2) 몰딩 조합 규칙 검증
2.7의 "최소 개수 → 최소 자투리" 규칙이 실제 발주 관행과 맞는지 확인 필요.
특히 2440 여러 개 vs 1200 여러 개가 갈리는 구간.

### (3) 몰딩 종류
민자 외 종류가 있는지 미확인. 하나뿐이면 선택 UI 불필요.

### (4) 입력 검증
최소/최대 치수 제한 없음으로 확인됨. 처리 완료:
- 세로 입력이 15mm 이하일 때 (몰딩 차감 시 0 이하) → `CUT_LENGTH_TOO_SMALL`
- 세로 입력이 원장 유효길이 초과일 때 → 오류 아님. 나눠서 이어 붙인다(2.2b)
- 0 또는 음수 입력 → `INVALID_WIDTH` / `INVALID_HEIGHT`
- 그 모양에 없는 색상 → `COLOR_NOT_IN_SHAPE`

### (5) 재단 시각화 — 완료
작업지시 도면을 페이지에 넣었다. 도면 데이터는 `cutting.js` 가 계산 결과에서 그대로 뽑고
(`barGroupsOf` / `wallLayout`), 그리기는 UI 쪽에서만 한다. 숫자와 그림이 어긋나지 않게
계산에 쓴 버킷을 그대로 쓴다.

- **벽면 배치도**: 가로는 자재 열(원장 + 100 낱개), 세로는 조각(이음). 상단 몰딩 표시.
  열 폭 합 = 가로, 행 길이 합 = 세로 (테스트로 검증)
- **재단 도면**: 자재 1개에 조각이 어떻게 들어가는지. 커프·남는 길이·끝단정리 표시.
  같은 모양의 자재는 묶어서 `× n개` 로 낸다
- **마감몰딩 도면**: 주문한 몰딩에서 쓰는 길이와 남는 길이
- 자투리는 결과물과 칸을 떼어 그리고 `버림` 이라고 적는다
- **도면은 세 묶음으로 나눈다.**
  1. `완성 배치` — 항목별 벽면 배치도 (무엇이 나오는지)
  2. `재단 방법` — 자재별 재단 도면 + 마감몰딩 도면 (어떻게 자르는지)
  3. `자투리` — `leftoverPieces()` 로 같은 크기·같은 출처끼리 모아, 남는 조각을 실제 비율로
- **제품 색을 칠한다.** 코드별 대표색은 `colors.js` 의 `CODE_SWATCH`.
  낱개는 같은 색에 빗금을 얹어 원장과 구분하고, 몰딩 띠는 같은 색을 살짝 어둡게 쓴다.
  칠한 색의 밝기에 따라 글자색을 뒤집어 대비를 맞춘다.
  ⚠️ 대표색은 색상 카드 이미지에서 눈으로 뽑은 값이다. 실물과 다르면 `CODE_SWATCH` 만 고친다.

---

## 6. 기술 스택

- 단일 HTML 파일 + 바닐라 JS. 빌드 도구 없음.
- 재단 계산 로직은 `cutting.js`로 분리한다. UI 코드와 섞지 않는다.
- 색상 목록은 하드코딩하지 말고 별도 배열/JSON으로 분리한다.
- 4번 테스트 케이스는 반드시 테스트 코드로 작성한다.
- 브라우저 저장소(localStorage 등) 사용 여부는 추후 결정.

## 7. 배포

`node build.js` 가 같은 HTML 을 두 곳에 쓴다.

| 경로 | 용도 | git |
|---|---|---|
| `index.html` (루트) | 배포가 서빙하는 파일. **항상 루트에 있어야 한다** | 커밋 |
| `dist/index.html` | 아티팩트 게시. 링크가 이 경로에 묶여 있다 | 제외 |

페이지를 고쳤으면 `node build.js` 를 돌려 루트 `index.html` 까지 갱신한 뒤 커밋한다.
빌드를 건너뛰면 커밋은 돼도 배포된 화면은 안 바뀐다.

## 8. 작업 원칙

- **미확정 규칙을 임의로 추측해 구현하지 말 것.** 5번 항목은 확인 후 진행한다.
- 계산 로직 변경 시 4번 테스트 케이스가 모두 통과하는지 확인한다.
- 도면은 계산 결과에서 뽑는다. 그리기용으로 수치를 다시 계산하지 않는다.

---

## 9. 단가

`pricing.js` 에 분리한다. 계산 로직과 섞지 않는다.

| 항목 | 단가 |
|---|---|
| 300 반달템바 (원장) | 40,500원 |
| 300 사각템바 (원장) | 43,000원 |
| 반달템바 100x2440x9T | 16,000원 |
| 사각템바 100x2440x12T | 16,000원 |
| 마감몰딩 2440mm 기준 | 8,000원 |
| 마감몰딩 1200mm 기준 | 4,000원 |
| 재단 필요해요 | 500원 (1회당) |

- **원장 단가는 색상과 무관하다.**
- 단가를 모르는 항목은 `null` 로 두고, 견적에 '단가 미정'으로 적고 합계에서 뺀다.
- 견적 줄 구성과 순서는 주문 요약과 똑같이 맞춘다.
